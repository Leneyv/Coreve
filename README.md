# Coreve

WordPress + WooCommerce store, running locally via Docker Compose.

## Local development

1. Copy `.env.example` to `.env` and fill in real values (or keep the local dev defaults).
2. `docker compose up -d`
3. Site: http://localhost:8090
4. Admin: http://localhost:8090/wp-admin
